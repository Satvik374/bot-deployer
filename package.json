{
  "name": "node.js-bots-deployment",
  "version": "1.0.0",
  "description": "Unified runner for BotDeploy",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently \"node backend/index.js\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}